import 'package:flutter/material.dart';

abstract class AppColors {
  // Principais
  static const blue = Color(0xFF0755B8);
  static const green = Color(0xFF69BE45);

  // Fundos
  static const white = Colors.white;
  static const background = Color(0xFFF7F9FC);
  static const surface = Colors.white;

  // Textos
  static const textPrimary = Color(0xFF17365D);
  static const textSecondary = Color(0xFF667085);

  // Auxiliares
  static const border = Color(0xFFD9E2EC);
  static const lightBlue = Color(0xFFEAF3FB);
  static const lightGreen = Color(0xFFEAF6E8);
}
