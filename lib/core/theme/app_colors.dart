import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const Color primary = Color(0xFF0757A6);
  static const Color secondary = Color(0xFF4CAF3D);

  // Fundos
  static const Color background = Color(0xFFF7F9FC);
  static const Color surface = Colors.white;

  // Textos
  static const Color textPrimary = Color(0xFF17365D);
  static const Color textSecondary = Color(0xFF667085);

  // Elementos auxiliares
  static const Color border = Color(0xFFD9E2EC);
  static const Color lightBlue = Color(0xFFEAF3FB);
  static const Color lightGreen = Color(0xFFEAF6E8);
}
